name: Flujo de trabajo para la pregunta 12
on:
  workflow_dispatch:
  push:
  issues:
    types: [opened, transferred]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 El nombre del flujo de trabajo es ${{ github.workflow }}."
 
